# Extracted from the installer at setup time — reproducible, don't commit.
via-extracted/
via-app/

# The Windows installer; download from upstream releases (see WINE_ON_LINUX_CACHYOS.md).
via-1.3.1-win.exe
